[project]
name = "resume"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pypdf>=6.14.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
